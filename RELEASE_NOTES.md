# Release Notes

Version v0.3.4 — June 29, 2026

## Fixes
- Really fixed the phantom update dot this time. A manually-installed app (one whose version AxiOM can't read from the filename) was always treated as "out of date," so the red dot stayed lit even though the app list showed nothing to update. Those now count as up to date until there's an actual newer release.
