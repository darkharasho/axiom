# Release Notes

Version v0.1.1 — April 23, 2026

## Windows install improvements

App updates on Windows now run silently through AxiOM — no installer window appears and the app no longer auto-launches after updating. The install flow now mirrors the Linux experience.

Fixed a bug where Windows apps showed a pending update immediately after a successful update was installed.
